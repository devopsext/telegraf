[agent]
  interval="5s"
  flush_interval="5s"
  quiet = true
  debug = false

[[outputs.cloudwatch]]
 region = "eu-central-1"
 namespace = "Logs/wta"
 access_key = "AKIASJMCNSIO4VJLTSUG"
 secret_key = "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"

[[inputs.docker_cnt_logs]]
  interval = "500ms"
  # Docker Endpoint
  #  To use unix, set endpoint = "unix:///var/run/docker.sock" (/var/run/docker.sock is default mount path)
  #  To use TCP, set endpoint = "tcp://[ip]:[port]"
  #  To use environment variables (ie, docker-machine), set endpoint = "ENV"
  endpoint = "unix:///var/run/docker.sock"

  # Set container id (long or short from), or container name
  # to stream logs from
  cont_id = "a72e458516b8"

  # Set initial chunk size (length of []byte buffer to read from docker socket)
  # If not set, default value of 'defaultInitialChunkSize = 10000' will be used
  initial_chunk_size = 10000 # 1 Mb

  # Set max chunk size (length of []byte buffer to read from docker socket)
  # If not set, default value of 'defaultMaxChunkSize = 50000' will be used
  # buffer can grow in capacity adjusting to volume of data received from docker sock
  max_chunk_size = 50000 # 5 Mb

  [inputs.docker_cnt_logs.tags]
    service = "collector"

#[[inputs.procstat]]
#  pid_finder = "parent"
#  pattern = "-bash"
#  tagexclude = ["pidfile","exe","pattern","user","systemd_unit","cgroup","win_service","container_id","container_name","namespace","environment","pod_name","version","type","source","location"]
#  namedrop = ["cpu_time_*","involuntary_context_switches","memory_data","memory_locked","memory_stack","memory_swap","memory_vms","nice_priority","num_*","read_*","write_*","realtime_priority","rlimit_*","signals_pending","voluntary_context_switches"]

[[outputs.file]]
  files = ["stdout"]
  data_format = "influx"

